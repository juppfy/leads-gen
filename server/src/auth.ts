// Kept for backward compatibility with older imports.
// All auth routes now live in `server/src/routes/auth.ts` and session validation
// is handled by `server/src/middleware/authMiddleware.ts`.
export const auth = {};
